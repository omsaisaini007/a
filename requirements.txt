# ============================================================
# FIFA Oracle — Python Backend Requirements
# ============================================================
# These dependencies power the optional FastAPI service that wraps
# the trained scikit-learn / XGBoost / LightGBM ensemble and exposes
# /predict-match and /simulate-tournament endpoints.
#
# Install with:  pip install -r requirements.txt
# ============================================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12

# --- ML core ---
scikit-learn==1.5.2
xgboost==2.1.1
lightgbm==4.5.0
numpy==2.1.1
pandas==2.2.3
scipy==1.14.1

# --- Hyperparameter optimization ---
optuna==4.0.0

# --- Calibration ---
# (CalibratedClassifierCV ships with scikit-learn — no extra dep)

# --- Persistence ---
joblib==1.4.2

# --- Async / parallel simulation ---
concurrent-futures  # stdlib, listed for clarity
multiprocess==0.70.17  # drop-in replacement for stdlib multiprocessing with better pickle support

# --- Data acquisition ---
httpx==0.27.2  # for fetching live FIFA rankings / Transfermarkt market values
beautifulsoup4==4.12.3

# --- Dashboard (alternative to Next.js for Python-only deployment) ---
streamlit==1.39.0
plotly==5.24.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Dev / docs ---
ruff==0.6.9
mypy==1.11.2
